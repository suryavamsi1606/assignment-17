#include<stdio.h>
main()
{
    int a,b,c;
    printf("enter the values of a,b,c");
    scanf("%d%d%d",&a,&b,&c);
    if(a<b&&a<c)
    {
	 printf("%d is lowest",a);	
	}	
	else if(b<a&&b<c)
	{
		printf("%d is lowest",b);
	}
	else
	{
		printf("%d is lowest",c);
	}
 }
