#include<stdio.h>
main()
{
	int n,i=1,c=0;
	printf("enter the value of n\n");
	scanf("%d",&n);
	while(i<=n)
	{
		if(n%i==0)
		{
		   c++;
		}
		i++;
	}
	if (c==2)
	printf("given number is prime");
	else
	printf("given number is not a prime");
}
